from setuptools import setup

setup(name='durparse',
      version='0.1',
      description='Parses duration strings into python `timedelta`',
      url='https://github.com/thedataincubator/durparse',
      license='MIT',
      packages=['durparse'],
      zip_safe=False)
